"use client"

import{useEffect,useMemo,useState}from"react"
import{supabase}from"../../../../lib/supabase"
import ReservationPaymentPanel from"./ReservationPaymentPanel"
import s from"./cashActions.module.css"

const money=(value,currency="ARS")=>new Intl.NumberFormat("es-AR",{style:"currency",currency,maximumFractionDigits:2}).format(Number(value)||0)
const fmt=value=>value?new Intl.DateTimeFormat("es-AR",{dateStyle:"short",timeStyle:"short"}).format(new Date(value)):"—"
const dual=(ars,usd)=>`${money(ars,"ARS")}${Math.abs(Number(usd)||0)>.009?` · ${money(usd,"USD")}`:""}`

export default function DailyCashActionsMultiCurrency({propertyId,session,expectedArs=0,expectedUsd=0,focusReservationId,onFocusHandled,onChanged}){
  const[paymentOpen,setPaymentOpen]=useState(false),[paymentReservationId,setPaymentReservationId]=useState(null),[mode,setMode]=useState(null),[saving,setSaving]=useState(false),[error,setError]=useState("")
  const[openingArs,setOpeningArs]=useState("0"),[openingUsd,setOpeningUsd]=useState("0"),[countedArs,setCountedArs]=useState(""),[countedUsd,setCountedUsd]=useState(""),[history,setHistory]=useState([])
  const[form,setForm]=useState({kind:"expense",other_direction:"expense",method:"Efectivo",amount:"",currency:"ARS",concept:"",reference:""})
  const differenceArs=Number(countedArs||0)-Number(expectedArs||0),differenceUsd=Number(countedUsd||0)-Number(expectedUsd||0)

  useEffect(()=>{if(!focusReservationId)return;setPaymentReservationId(Number(focusReservationId));setPaymentOpen(true);onFocusHandled?.()},[focusReservationId,onFocusHandled])
  useEffect(()=>{if(!mode||!propertyId)return;let cancelled=false;(async()=>{const res=await supabase.from("hotel_cash_sessions").select("id,opened_at,closed_at,opening_amount,opening_amount_usd,closing_amount,closing_amount_usd,expected_amount,expected_amount_usd,status").eq("property_id",propertyId).eq("status","closed").order("closed_at",{ascending:false}).limit(8);if(!cancelled&&!res.error)setHistory(res.data||[])})();return()=>{cancelled=true}},[mode,propertyId])
  useEffect(()=>{if(!session){setCountedArs("");setCountedUsd("")}},[session?.id])

  async function currentUser(){const{data,error:userError}=await supabase.auth.getUser();if(userError)throw userError;if(!data?.user)throw new Error("No se pudo identificar al usuario actual.");return data.user}
  function finish(){setMode(null);setError("");onChanged?.()}

  async function openSession(){setSaving(true);setError("");try{const ars=Number(openingArs||0),usd=Number(openingUsd||0);if(!Number.isFinite(ars)||ars<0||!Number.isFinite(usd)||usd<0)throw new Error("Ingresá montos iniciales válidos.");const user=await currentUser();const{error:insertError}=await supabase.from("hotel_cash_sessions").insert({property_id:propertyId,opened_by:user.id,closed_by:null,opened_at:new Date().toISOString(),closed_at:null,opening_amount:ars,opening_amount_usd:usd,closing_amount:null,closing_amount_usd:null,expected_amount:null,expected_amount_usd:null,status:"open",notes:null});if(insertError)throw insertError;if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-toast",{detail:{title:"Caja abierta",message:`Apertura ${dual(ars,usd)}.`}}));setOpeningArs("0");setOpeningUsd("0");finish()}catch(err){setError(err?.message||"No se pudo abrir la caja.")}finally{setSaving(false)}}

  async function addMovement(){setSaving(true);setError("");try{if(!session)throw new Error("Abrí una caja antes de registrar movimientos.");const amount=Number(form.amount||0);if(!Number.isFinite(amount)||amount<=0)throw new Error("Ingresá un importe válido.");if(!form.concept.trim())throw new Error("Ingresá el concepto del movimiento.");const movementType=form.kind==="income"?"income":form.kind==="refund"?"refund":form.kind==="other"?form.other_direction:"expense";const user=await currentUser();const{error:insertError}=await supabase.from("hotel_cash_movements").insert({property_id:propertyId,session_id:session.id,reservation_id:null,movement_type:movementType,method:form.method,amount,currency:form.currency,concept:form.concept.trim(),reference:form.reference.trim()||null,created_by:user.id});if(insertError)throw insertError;if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-toast",{detail:{title:"Movimiento registrado",message:`${form.concept.trim()} · ${money(amount,form.currency)}.`}}));setForm(current=>({...current,amount:"",concept:"",reference:""}));finish()}catch(err){setError(err?.message||"No se pudo registrar el movimiento.")}finally{setSaving(false)}}

  async function closeSession(){setSaving(true);setError("");try{if(!session)throw new Error("No hay una caja abierta.");if(countedArs===""||countedUsd===""||Number(countedArs)<0||Number(countedUsd)<0)throw new Error("Contá el efectivo en ARS y USD. Si no hay dólares, ingresá 0.");const user=await currentUser(),ars=Number(countedArs),usd=Number(countedUsd);const{error:updateError}=await supabase.from("hotel_cash_sessions").update({closed_by:user.id,closed_at:new Date().toISOString(),closing_amount:ars,closing_amount_usd:usd,expected_amount:Number(expectedArs||0),expected_amount_usd:Number(expectedUsd||0),status:"closed"}).eq("id",session.id).eq("property_id",propertyId);if(updateError)throw updateError;if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-toast",{detail:{title:"Caja cerrada",message:`Esperado ${dual(expectedArs,expectedUsd)} · contado ${dual(ars,usd)}.`}}));setCountedArs("");setCountedUsd("");finish()}catch(err){setError(err?.message||"No se pudo cerrar la caja.")}finally{setSaving(false)}}

  const title=mode==="open"?"Abrir caja":mode==="movement"?"Registrar movimiento":"Arqueo y cierre de caja"
  const subtitle=mode==="open"?"Ingresá el efectivo inicial separado por moneda.":mode==="movement"?"Ingresos y egresos manuales quedan vinculados al turno abierto.":"Contá ARS y USD por separado y comparalos contra el esperado del sistema."
  const recent=useMemo(()=>history.map(row=>({...row,diffArs:Number(row.closing_amount||0)-Number(row.expected_amount||0),diffUsd:Number(row.closing_amount_usd||0)-Number(row.expected_amount_usd||0)})),[history])
  const conceptPlaceholder=form.kind==="other"?"Describí el otro tipo de movimiento…":form.kind==="income"?"Ej. ingreso adicional, aporte a caja…":form.kind==="refund"?"Ej. devolución a huésped…":"Ej. compra de insumos, retiro, proveedor…"
  const closeReady=countedArs!==""&&countedUsd!==""

  return <>
    <div className={s.actions}><button type="button" className={s.action} onClick={()=>{setPaymentReservationId(null);setPaymentOpen(true)}}>＋ Cobrar reserva</button><button type="button" className={s.action} onClick={()=>{setError("");setMode(session?"movement":"open")}}>{session?"＋ Movimiento":"＋ Abrir caja"}</button><button type="button" className={s.primary} onClick={()=>{setError("");setMode(session?"close":"open")}}>{session?"Arqueo / cerrar caja":"Abrir caja"}</button></div>
    {paymentOpen?<ReservationPaymentPanel propertyId={propertyId} reservationId={paymentReservationId} session={session} onClose={()=>{setPaymentOpen(false);setPaymentReservationId(null)}} onSaved={()=>{setPaymentOpen(false);setPaymentReservationId(null);onChanged?.()}}/>:null}
    {mode?<div className={s.overlay} role="dialog" aria-modal="true" aria-label={title}><section className={s.panel}><header className={s.panelHeader}><div><small>CAJA DIARIA · TURNO MULTIMONEDA</small><h2>{title}</h2><p>{subtitle}</p></div><button type="button" className={s.close} onClick={()=>setMode(null)}>×</button></header><div className={s.body}>{error?<div className={s.alert}>{error}</div>:null}
      {mode==="open"?<><p className={s.hint}>Los cobros en efectivo de reservas se suman automáticamente en la moneda realmente recibida.</p><div className={s.formGrid}><label className={s.field}><span>Efectivo inicial ARS</span><input type="number" min="0" step="0.01" value={openingArs} onChange={event=>setOpeningArs(event.target.value)} autoFocus/></label><label className={s.field}><span>Efectivo inicial USD</span><input type="number" min="0" step="0.01" value={openingUsd} onChange={event=>setOpeningUsd(event.target.value)}/></label></div><div className={s.footer}><button type="button" className={s.secondary} onClick={()=>setMode(null)}>Cancelar</button><button type="button" className={s.save} disabled={saving} onClick={openSession}>{saving?"Abriendo…":"Abrir caja"}</button></div></>:mode==="movement"?<><div className={s.formGrid}><label className={s.field}><span>Tipo</span><select value={form.kind} onChange={event=>setForm(current=>({...current,kind:event.target.value}))}><option value="income">Ingreso</option><option value="expense">Egreso</option><option value="spending">Gasto</option><option value="refund">Devolución</option><option value="other">Otro</option></select></label>{form.kind==="other"?<label className={s.field}><span>Impacta como</span><select value={form.other_direction} onChange={event=>setForm(current=>({...current,other_direction:event.target.value}))}><option value="income">Ingreso</option><option value="expense">Egreso</option></select></label>:<label className={s.field}><span>Medio</span><select value={form.method} onChange={event=>setForm(current=>({...current,method:event.target.value}))}><option>Efectivo</option><option>Transferencia</option><option>Mercado Pago</option><option>Tarjeta</option><option>Otro</option></select></label>}{form.kind==="other"?<label className={s.field}><span>Medio</span><select value={form.method} onChange={event=>setForm(current=>({...current,method:event.target.value}))}><option>Efectivo</option><option>Transferencia</option><option>Mercado Pago</option><option>Tarjeta</option><option>Otro</option></select></label>:null}<label className={s.field}><span>Importe</span><input type="number" min="0.01" step="0.01" value={form.amount} onChange={event=>setForm(current=>({...current,amount:event.target.value}))}/></label><label className={s.field}><span>Moneda</span><select value={form.currency} onChange={event=>setForm(current=>({...current,currency:event.target.value}))}><option>ARS</option><option>USD</option></select></label><label className={`${s.field} ${s.fieldFull}`}><span>Concepto{form.kind==="other"?" · especificá cuál":""}</span><input value={form.concept} onChange={event=>setForm(current=>({...current,concept:event.target.value}))} placeholder={conceptPlaceholder}/></label><label className={`${s.field} ${s.fieldFull}`}><span>Referencia</span><input value={form.reference} onChange={event=>setForm(current=>({...current,reference:event.target.value}))} placeholder="Opcional"/></label></div><div className={s.footer}><button type="button" className={s.secondary} onClick={()=>setMode(null)}>Cancelar</button><button type="button" className={s.save} disabled={saving} onClick={addMovement}>{saving?"Registrando…":"Registrar movimiento"}</button></div></>:<><div className={s.reconcile}><div className={s.reconcileRow}><span>Efectivo esperado ARS</span><b>{money(expectedArs,"ARS")}</b></div><label className={s.field}><span>Efectivo contado ARS</span><input type="number" min="0" step="0.01" value={countedArs} onChange={event=>setCountedArs(event.target.value)} autoFocus/></label><div className={s.reconcileRow}><span>Diferencia ARS</span><b className={Math.abs(differenceArs)<.01?s.good:s.bad}>{countedArs===""?"—":money(differenceArs,"ARS")}</b></div><div className={s.reconcileRow}><span>Efectivo esperado USD</span><b>{money(expectedUsd,"USD")}</b></div><label className={s.field}><span>Efectivo contado USD</span><input type="number" min="0" step="0.01" value={countedUsd} onChange={event=>setCountedUsd(event.target.value)} placeholder="0"/></label><div className={s.reconcileRow}><span>Diferencia USD</span><b className={Math.abs(differenceUsd)<.01?s.good:s.bad}>{countedUsd===""?"—":money(differenceUsd,"USD")}</b></div></div><div className={s.footer}><button type="button" className={s.secondary} onClick={()=>setMode(null)}>Cancelar</button><button type="button" className={s.danger} disabled={saving||!closeReady} onClick={closeSession}>{saving?"Cerrando…":"Cerrar caja"}</button></div></>}
      {recent.length?<div className={s.history}><h3>Últimos cierres</h3><div className={s.historyRows}><div className={`${s.historyRow} ${s.historyHead}`}><span>Turno</span><span>Apertura</span><span>Esperado</span><span>Contado</span><span>Diferencia</span></div>{recent.map(row=><div className={s.historyRow} key={row.id}><span><b>{fmt(row.closed_at)}</b><small>Abierta {fmt(row.opened_at)}</small></span><span>{dual(row.opening_amount,row.opening_amount_usd)}</span><span>{dual(row.expected_amount,row.expected_amount_usd)}</span><span>{dual(row.closing_amount,row.closing_amount_usd)}</span><strong className={Math.abs(row.diffArs)<.01&&Math.abs(row.diffUsd)<.01?s.good:s.bad}>{dual(row.diffArs,row.diffUsd)}</strong></div>)}</div></div>:null}
    </div></section></div>:null}
  </>
}
