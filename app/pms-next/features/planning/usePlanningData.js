"use client"

import{useCallback,useEffect,useState}from"react"
import{supabase}from"../../../../lib/supabase"
import{attachPayments}from"./planningPayment"
import{normalizeTaxSettings,roundInternalPrice,roundPrice}from"../../core/priceTax"
import{pricingRoomsById,resolvePricingQuote}from"../../core/pricingContract"

const DAY=86400000
const nightsBetween=(start,end)=>Math.max(1,Math.round((new Date(`${end}T12:00:00`)-new Date(`${start}T12:00:00`))/DAY))
const money=(value,currency="ARS")=>new Intl.NumberFormat("es-AR",{style:"currency",currency:currency||"ARS",maximumFractionDigits:0}).format(Number(value)||0)
function toast(detail){if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-toast",{detail}))}
const uniqueNumeric=values=>[...new Set((values||[]).map(Number).filter(Number.isFinite))]
const reservationRooms=item=>uniqueNumeric([...(item?._reservation_room_ids||[]),item?.habitacion_id,...(item?.habitaciones_ids||[])])
const validDate=value=>/^\d{4}-\d{2}-\d{2}$/.test(String(value||""))
const shiftDate=(value,amount)=>{const d=new Date(`${value}T12:00:00`);d.setDate(d.getDate()+amount);return d.toISOString().slice(0,10)}
const explicitBool=(obj,key)=>Object.prototype.hasOwnProperty.call(obj||{},key)?String(obj[key]).toLowerCase()==="true":null
const roomDetail=(item,roomId)=>(Array.isArray(item?.habitaciones_detalle)?item.habitaciones_detalle:[]).find(row=>Number(row?.habitacion_id)===Number(roomId))||{}
const roomStart=(item,roomId)=>{const value=roomDetail(item,roomId)?.fecha_entrada;return validDate(value)?String(value):item?.fecha_entrada}
const roomPlannedEnd=(item,roomId)=>{const value=roomDetail(item,roomId)?.fecha_salida;return validDate(value)?String(value):item?.fecha_salida}
const roomCheckoutDate=(item,roomId)=>{const value=item?.room_checkout_dates?.[String(roomId)];return validDate(value)?String(value):""}
const effectiveRoomEnd=(item,roomId)=>{const planned=roomPlannedEnd(item,roomId),release=roomCheckoutDate(item,roomId);return release&&release<planned?release:planned}
const stayDateKeys=(start,end)=>{const result=[],cursor=new Date(`${start}T12:00:00`),limit=new Date(`${end}T12:00:00`);while(cursor<limit){result.push(cursor.toISOString().slice(0,10));cursor.setDate(cursor.getDate()+1)}return result}
const normalizeBlockReason=value=>String(value||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").trim().toLowerCase()
const isMaintenanceBlock=block=>normalizeBlockReason(block?.motivo)==="mantenimiento"
const maintenanceAckKey=blocks=>(blocks||[]).filter(isMaintenanceBlock).map(block=>`${block.id}:${block.habitacion_id}:${block.fecha_desde}`).sort().join("|")
const planningSegments=rows=>(rows||[]).flatMap(item=>{
  const ids=reservationRooms(item);if(!ids.length)return[item]
  return ids.flatMap(id=>{const start=roomStart(item,id),planned=roomPlannedEnd(item,id),end=effectiveRoomEnd(item,id),detail=roomDetail(item,id);if(!start||!end||end<=start)return[];const earlyFlag=explicitBool(detail,"early_checkin_requested"),lateFlag=explicitBool(detail,"late_checkout_requested"),early=earlyFlag??Boolean(detail?.early_checkin_time||Number(detail?.early_checkin_net)>0||((Boolean(item.early_checkin)||Number(item.early_checkin_importe)>0)&&start===item.fecha_entrada)),late=Boolean(String(item?.room_checkout_dates?.[`late:${id}`]||"").slice(0,10))||(lateFlag??Boolean(detail?.late_checkout_time||Number(detail?.late_checkout_net)>0||((Boolean(item.late_checkout)||Number(item.late_checkout_importe)>0)&&planned===item.fecha_salida)));return[{...item,habitacion_id:id,habitaciones_ids:[id],fecha_entrada:start,fecha_salida:end,_reservation_start:item.fecha_entrada,_reservation_end:item.fecha_salida,_reservation_room_ids:ids,_room_detail:detail,_room_segment:true,_room_early:early,_room_late:late,_room_checkout_date:roomCheckoutDate(item,id)||null}]})
})
const policySnapshot=policy=>policy?{id:policy.id,code:policy.code,name:policy.name,description:policy.description,policy_type:policy.policy_type,language:policy.language,currency:policy.currency,cancellation_rules:policy.cancellation_rules||[],no_show_rule:policy.no_show_rule||{charge_type:"none",value:0},early_checkout_rule:policy.early_checkout_rule||{charge_type:"none",value:0},prepayment_required:Boolean(policy.prepayment_required),prepayment_percent:Number(policy.prepayment_percent)||0,captured_at:new Date().toISOString()}:{}
const DISCOUNT_REASON_LABELS={group:"Grupo / varias habitaciones",long_stay:"Estadía prolongada",promotion:"Promoción comercial",commercial:"Acuerdo comercial",loyalty:"Fidelización",courtesy:"Cortesía",other:"Otro"}

export default function usePlanningData(propertyId,windowStart,windowEndExclusive){
  const[rooms,setRooms]=useState([]),[reservations,setReservations]=useState([]),[blocks,setBlocks]=useState([]),[cancellationPolicies,setCancellationPolicies]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("")
  const load=useCallback(async(silent=false)=>{
    if(!propertyId||!windowStart||!windowEndExclusive)return;if(!silent)setLoading(true);setError("")
    try{
      const[roomRes,resRes,floorRes,paymentRes,policyRes,blockRes]=await Promise.all([
        supabase.from("habitaciones").select("id,nombre,tipo,capacidad,precio,estado,activa,sort_order,housekeeping_zone,floor_id").eq("property_id",propertyId).eq("activa",true),
        supabase.from("reservas").select("id,numero_reserva,nombre_huesped,email_huesped,telefono_huesped,habitacion_id,habitaciones_ids,habitaciones_detalle,room_checkout_dates,fecha_entrada,fecha_salida,estado,tarifa_noche,precio_total,precio_sin_impuestos_nacionales,iva_porcentaje,iva_importe,impuestos_desglosados,condicion_iva_huesped,moneda,canal_reserva,cantidad_huespedes,no_show,tipo_estadia,notas,cancellation_policy_id,cancellation_policy_snapshot,early_checkin,early_checkin_importe,late_checkout,late_checkout_importe").eq("property_id",propertyId).neq("estado","cancelada").lt("fecha_entrada",shiftDate(windowEndExclusive,1)).gt("fecha_salida",shiftDate(windowStart,-1)).order("fecha_entrada"),
        supabase.from("hotel_floors").select("id,name,sort_order,active").eq("property_id",propertyId).eq("active",true).order("sort_order"),
        supabase.from("pagos").select("id,reserva_id,monto,moneda,estado,created_at").eq("property_id",propertyId).eq("estado","confirmado"),
        supabase.from("hotel_cancellation_policies").select("id,code,name,description,policy_type,language,currency,cancellation_rules,no_show_rule,early_checkout_rule,prepayment_required,prepayment_percent,active,is_default").eq("property_id",propertyId).eq("active",true).order("is_default",{ascending:false}).order("name"),
        supabase.from("bloqueos").select("id,habitacion_id,fecha_desde,fecha_hasta,motivo,detalle").eq("property_id",propertyId).lt("fecha_desde",windowEndExclusive).gt("fecha_hasta",windowStart).order("fecha_desde"),
      ])
      if(roomRes.error)throw roomRes.error;if(resRes.error)throw resRes.error;if(floorRes.error)throw floorRes.error;if(policyRes.error)throw policyRes.error;if(blockRes.error)throw blockRes.error
      const reservationRows=resRes.data||[],reservationIds=reservationRows.map(row=>Number(row.id)).filter(Number.isFinite)
      let guestRows=[]
      if(reservationIds.length){
        const guestRes=await supabase.from("hotel_reservation_guests").select("reservation_id,room_id,checked_out_at").eq("property_id",propertyId).in("reservation_id",reservationIds)
        if(guestRes.error)throw guestRes.error
        guestRows=guestRes.data||[]
      }
      const manifestReservations=new Set(guestRows.map(row=>Number(row.reservation_id)).filter(Number.isFinite)),activeGuestsByRoom=new Map(),unassignedActiveGuests=new Map()
      for(const guest of guestRows){
        if(guest.checked_out_at)continue
        const reservationId=Number(guest.reservation_id),roomId=Number(guest.room_id)
        if(Number.isFinite(roomId))activeGuestsByRoom.set(`${reservationId}:${roomId}`,(activeGuestsByRoom.get(`${reservationId}:${roomId}`)||0)+1)
        else unassignedActiveGuests.set(reservationId,(unassignedActiveGuests.get(reservationId)||0)+1)
      }
      const segments=planningSegments(reservationRows).map(segment=>{
        const reservationId=Number(segment.id),roomId=Number(segment.habitacion_id),hasManifest=manifestReservations.has(reservationId),singleRoom=reservationRooms(segment).length===1
        const roomGuests=activeGuestsByRoom.get(`${reservationId}:${roomId}`)||0,unassigned=singleRoom?(unassignedActiveGuests.get(reservationId)||0):0
        return{...segment,_has_guest_manifest:hasManifest,_room_guest_count:hasManifest?roomGuests+unassigned:null}
      })
      const floorById=new Map((floorRes.data||[]).map(floor=>[String(floor.id),floor])),roomRows=(roomRes.data||[]).map(room=>{const floor=floorById.get(String(room.floor_id||""));return{...room,floor_name:floor?.name||"Sin piso",floor_sort:Number(floor?.sort_order??999)}}).sort((a,b)=>a.floor_sort-b.floor_sort||Number(a.sort_order||0)-Number(b.sort_order||0)||String(a.nombre).localeCompare(String(b.nombre),"es",{numeric:true})),enriched=attachPayments(segments,paymentRes.error?[]:paymentRes.data||[])
      setRooms(roomRows);setReservations(enriched);setBlocks(blockRes.data||[]);setCancellationPolicies(policyRes.data||[])
    }catch(err){setError(err?.message||"No se pudo cargar el Planning.")}finally{if(!silent)setLoading(false)}
  },[propertyId,windowStart,windowEndExclusive])
  useEffect(()=>{load()},[load])
  useEffect(()=>{if(typeof window==="undefined")return;const refresh=()=>load(true);window.addEventListener("hl:pms-payment-updated",refresh);window.addEventListener("hl:pms-reservation-updated",refresh);window.addEventListener("hl:pms-cancellation-policies-updated",refresh);return()=>{window.removeEventListener("hl:pms-payment-updated",refresh);window.removeEventListener("hl:pms-reservation-updated",refresh);window.removeEventListener("hl:pms-cancellation-policies-updated",refresh)}},[load])
  useEffect(()=>{if(!propertyId)return;let timer=null;const refresh=()=>{if(timer)clearTimeout(timer);timer=setTimeout(()=>load(true),70)},channel=supabase.channel(`hl-planning-live-${propertyId}`).on("postgres_changes",{event:"*",schema:"public",table:"reservas",filter:`property_id=eq.${propertyId}`},refresh).on("postgres_changes",{event:"*",schema:"public",table:"pagos",filter:`property_id=eq.${propertyId}`},refresh).on("postgres_changes",{event:"*",schema:"public",table:"hotel_cancellation_policies",filter:`property_id=eq.${propertyId}`},refresh).on("postgres_changes",{event:"*",schema:"public",table:"hotel_reservation_guests",filter:`property_id=eq.${propertyId}`},refresh).on("postgres_changes",{event:"*",schema:"public",table:"bloqueos",filter:`property_id=eq.${propertyId}`},refresh).subscribe();return()=>{if(timer)clearTimeout(timer);supabase.removeChannel(channel)}},[propertyId,load])

  const moveReservation=useCallback(async({reservationId,roomId,start,end,reprice=false})=>{
    const numericId=Number(reservationId),numericRoom=Number(roomId),previous=reservations.find(item=>Number(item.id)===numericId);if(!previous)throw new Error("No encontramos la reserva en el Planning actual.")
    const oldNights=nightsBetween(previous.fecha_entrada,previous.fecha_salida),newNights=nightsBetween(start,end),optimistic={...previous,habitacion_id:numericRoom,habitaciones_ids:[numericRoom],fecha_entrada:start,fecha_salida:end,noches:newNights};setReservations(list=>list.map(item=>Number(item.id)===numericId?optimistic:item).filter(item=>item.fecha_entrada<windowEndExclusive&&item.fecha_salida>windowStart));setError("")
    try{const{data,error:rpcError}=await supabase.rpc("hl_planning_move_reservation_priced_atomic",{p_reserva_id:numericId,p_habitacion_id:numericRoom,p_fecha_entrada:start,p_fecha_salida:end,p_reprice:Boolean(reprice)});if(rpcError)throw rpcError;await load(true);const oldRoomData=rooms.find(room=>Number(room.id)===Number(previous.habitacion_id)),newRoomData=rooms.find(room=>Number(room.id)===numericRoom),oldRoom=oldRoomData?.nombre||previous.habitacion_id,newRoom=newRoomData?.nombre||numericRoom,roomChanged=Number(previous.habitacion_id)!==numericRoom,startChanged=previous.fecha_entrada!==start,durationChanged=oldNights!==newNights,categoryChanged=roomChanged&&String(oldRoomData?.tipo||"")!==String(newRoomData?.tipo||""),oldRate=Number(previous.tarifa_noche)||0,newRate=Number(data?.tarifa_noche)||0,currency=data?.moneda||previous.moneda||"ARS",direction=newRate>oldRate?"Upgrade":newRate<oldRate?"Downgrade":"Cambio de categoría";let message="Cambio guardado en el Planning.";if(categoryChanged)message=`${direction}: ${oldRoom} (${oldRoomData?.tipo||"sin categoría"}) → ${newRoom} (${newRoomData?.tipo||"sin categoría"}) · ${reprice&&oldRate!==newRate?`tarifa ${money(oldRate,currency)} → ${money(newRate,currency)}`:"tarifa original mantenida"}.`;else if(roomChanged&&startChanged)message=`Habitación ${oldRoom} → ${newRoom} · nueva entrada ${start}${reprice&&oldRate!==newRate?` · tarifa ${money(oldRate,currency)} → ${money(newRate,currency)}`:""}.`;else if(roomChanged)message=`Habitación ${oldRoom} → ${newRoom}${reprice&&oldRate!==newRate?` · tarifa ${money(oldRate,currency)} → ${money(newRate,currency)}`:""}.`;else if(startChanged)message=`Reserva movida a ${start}.`;if(!(durationChanged&&!roomChanged&&!startChanged))toast({title:"Planning actualizado",message});if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-reservation-updated",{detail:{reservationId:numericId}}));return data}catch(err){await load(true);toast({tone:"error",title:"Cambio revertido",message:"El servidor no pudo confirmar el movimiento y el Planning volvió al estado anterior.",duration:4200});throw err}
  },[reservations,rooms,windowStart,windowEndExclusive,load])

  const createReservation=useCallback(async draft=>{
    const{data:userData,error:userError}=await supabase.auth.getUser();if(userError)throw userError
    const roomIds=uniqueNumeric(draft.roomIds?.length?draft.roomIds:[draft.roomId]);if(!roomIds.length)throw new Error("Elegí al menos una habitación.")
    const selectedRooms=roomIds.map(id=>rooms.find(room=>Number(room.id)===id)).filter(Boolean);if(selectedRooms.length!==roomIds.length)throw new Error("Hay una habitación seleccionada que ya no está disponible en esta propiedad.")
    const policy=cancellationPolicies.find(row=>String(row.id)===String(draft.cancellationPolicyId))||cancellationPolicies.find(row=>row.is_default)||cancellationPolicies[0];if(!policy)throw new Error("Configurá una política de cancelación antes de crear la reserva.")
    const[conflictRes,blockRes,boundaryBlockRes]=await Promise.all([
      supabase.from("reservas").select("id,numero_reserva,habitacion_id,habitaciones_ids,habitaciones_detalle,room_checkout_dates,fecha_entrada,fecha_salida,estado,no_show").eq("property_id",propertyId).neq("estado","cancelada").eq("no_show",false).lt("fecha_entrada",draft.end).gt("fecha_salida",draft.start),
      supabase.from("bloqueos").select("id,habitacion_id,fecha_desde,fecha_hasta,motivo").eq("property_id",propertyId).in("habitacion_id",roomIds).lt("fecha_desde",draft.end).gt("fecha_hasta",draft.start),
      supabase.from("bloqueos").select("id,habitacion_id,fecha_desde,fecha_hasta,motivo").eq("property_id",propertyId).in("habitacion_id",roomIds).eq("fecha_desde",draft.end),
    ]);if(conflictRes.error)throw conflictRes.error;if(blockRes.error)throw blockRes.error;if(boundaryBlockRes.error)throw boundaryBlockRes.error
    const roomOverlaps=(item,id)=>reservationRooms(item).includes(id)&&roomStart(item,id)<draft.end&&effectiveRoomEnd(item,id)>draft.start,conflict=(conflictRes.data||[]).find(item=>roomIds.some(id=>roomOverlaps(item,id)));if(conflict){const conflictRoom=selectedRooms.find(room=>roomOverlaps(conflict,Number(room.id)));throw new Error(`La habitación ${conflictRoom?.nombre||"seleccionada"} ya tiene una reserva que se superpone con esas fechas.`)}
    const block=(blockRes.data||[])[0];if(block){const blockedRoom=selectedRooms.find(room=>Number(room.id)===Number(block.habitacion_id));throw new Error(`La habitación ${blockedRoom?.nombre||block.habitacion_id} tiene un bloqueo operativo${block.motivo?` (${block.motivo})`:""} durante esas fechas. Elegí otra habitación o liberá el bloqueo.`)}
    const boundaryBlocks=boundaryBlockRes.data||[],hardBoundary=boundaryBlocks.find(item=>!isMaintenanceBlock(item));if(hardBoundary){const blockedRoom=selectedRooms.find(room=>Number(room.id)===Number(hardBoundary.habitacion_id));throw new Error(`La habitación ${blockedRoom?.nombre||hardBoundary.habitacion_id} tiene un bloqueo operativo${hardBoundary.motivo?` (${hardBoundary.motivo})`:""} el día de salida. Ese bloqueo no admite superposición.`)}
    const maintenanceBoundary=boundaryBlocks.filter(isMaintenanceBlock),expectedMaintenanceAck=maintenanceAckKey(maintenanceBoundary);if(expectedMaintenanceAck&&draft.maintenanceCheckoutAckKey!==expectedMaintenanceAck){const first=maintenanceBoundary[0],blockedRoom=selectedRooms.find(room=>Number(room.id)===Number(first.habitacion_id));throw new Error(`La habitación ${blockedRoom?.nombre||first.habitacion_id} tiene mantenimiento programado el día de salida. Confirmá que se hará después del check-out para continuar.`)}
    const maintenanceByRoom=new Map(maintenanceBoundary.map(item=>[String(item.habitacion_id),item]))

    const roomAssignments=draft.roomAssignments||{},isGroup=roomIds.length>1,nights=nightsBetween(draft.start,draft.end),pricing=new Map()
    const resolvedPricing=await resolvePricingQuote({propertyId,start:draft.start,end:draft.end,roomIds,reservationCurrency:draft.currency||null})
    const resolvedByRoom=pricingRoomsById(resolvedPricing)
    for(const room of selectedRooms){
      const assignment=roomAssignments[String(room.id)]||{},soldAs=String(assignment.soldAs||room.tipo||"Habitación").trim(),manual=assignment.manualRate===true
      if(manual){const manualRate=Math.max(0,Number(assignment.rate)||0);pricing.set(Number(room.id),{rate:manualRate,total:roundInternalPrice(manualRate*nights),source:"manual",category:soldAs});continue}
      if(soldAs!==String(room.tipo||"Habitación").trim())throw new Error(`La categoría vendida ${soldAs} no coincide con la habitación ${room.nombre}. Revalidá la tarifa antes de crear la reserva.`)
      const quote=resolvedByRoom.get(Number(room.id))
      if(!quote?.is_complete)throw new Error(`Falta configurar una tarifa de calendario para ${room.nombre} en alguna noche del rango. Revisá Tarifas y disponibilidad.`)
      pricing.set(Number(room.id),{rate:Number(quote.average_net)||0,total:Number(quote.total_net)||0,source:quote.rate_source||"central_pricing",category:soldAs,pricingVersion:2})
    }
    const groupTaxes=normalizeTaxSettings({enabled:resolvedPricing.taxes_enabled,vat_rate:resolvedPricing.vat_rate,price_tax_mode:resolvedPricing.tax_mode})
    const requestedGuests=Math.max(1,Number(draft.guests)||1),details=selectedRooms.map(room=>{const assignment=roomAssignments[String(room.id)]||{},resolved=pricing.get(Number(room.id)),soldAs=String(assignment.soldAs||room.tipo||"Habitación").trim(),maintenanceBlock=maintenanceByRoom.get(String(room.id));return{habitacion_id:Number(room.id),nombre:room.nombre,categoria_asignada:room.tipo||"Habitación",categoria_vendida:soldAs,huespedes:Math.max(0,Number(assignment.guests)||0),tarifa_noche:resolved.rate,tarifa_fuente:resolved.source,tarifa_categoria:resolved.category,tarifa_manual:resolved.source==="manual",fecha_entrada:draft.start,fecha_salida:draft.end,rooming:{matrimonial:Math.max(0,Number(assignment.matrimonial)||0),individual:Math.max(0,Number(assignment.individual)||0)},...(maintenanceBlock?{maintenance_checkout_ack:true,maintenance_checkout_block_id:Number(maintenanceBlock.id),maintenance_checkout_date:draft.end,maintenance_checkout_ack_at:new Date().toISOString()}: {})}}),assignedGuests=details.reduce((sum,item)=>sum+item.huespedes,0);if(assignedGuests!==requestedGuests)throw new Error(`Distribuí los ${requestedGuests} huésped${requestedGuests===1?"":"es"} entre las habitaciones seleccionadas antes de crear la reserva.`)
    const totalGuests=requestedGuests,defaultRate=details.reduce((sum,item)=>sum+item.tarifa_noche,0),rate=roundInternalPrice(defaultRate),subtotal=roundInternalPrice([...pricing.values()].reduce((sum,item)=>sum+item.total,0)),discountType=draft.discountType||"none",discountValue=Math.max(0,Number(draft.discountValue)||0),discountAmount=discountType==="percent"?Math.min(subtotal,subtotal*Math.min(100,discountValue)/100):discountType==="amount"?Math.min(subtotal,discountValue):0,total=Math.max(0,subtotal-discountAmount),reasonKey=String(draft.discountReason||"").trim(),reasonDetail=String(draft.discountReasonDetail||"").trim();if(discountAmount>0&&!reasonKey)throw new Error("Indicá el motivo del descuento antes de crear la reserva.");if(discountAmount>0&&reasonKey==="other"&&!reasonDetail)throw new Error("Especificá el motivo del descuento.");const reasonLabel=DISCOUNT_REASON_LABELS[reasonKey]||reasonKey,discountReason=discountAmount>0?`${reasonLabel}${reasonDetail?` · ${reasonDetail}`:""}`:null
    let taxPayload={},finalTotal=total
    if(isGroup){const vatRate=groupTaxes?.enabled?Math.max(0,Number(groupTaxes.vat_rate)||0):0,vatAmount=groupTaxes?.enabled?roundPrice(total*vatRate/100):0;taxPayload={impuestos_desglosados:Boolean(groupTaxes?.enabled),iva_porcentaje:vatRate,precio_sin_impuestos_nacionales:total,iva_importe:vatAmount};finalTotal=roundPrice(total+vatAmount)}else{const hasTaxChoice=typeof draft.impuestosDesglosados==="boolean",vatRate=hasTaxChoice&&draft.impuestosDesglosados?Math.max(0,Number(draft.ivaPorcentaje)||0):0,vatAmount=hasTaxChoice&&draft.impuestosDesglosados?Math.round(total*vatRate)/100:0;taxPayload=hasTaxChoice?{impuestos_desglosados:draft.impuestosDesglosados,iva_porcentaje:vatRate,precio_sin_impuestos_nacionales:total,iva_importe:vatAmount}:{};finalTotal=total}
    const payload={property_id:propertyId,user_id:userData?.user?.id||null,guest_profile_id:draft.guestProfileId||null,habitacion_id:roomIds[0],habitaciones_ids:roomIds,habitaciones_detalle:details,fecha_entrada:draft.start,fecha_salida:draft.end,tipo_estadia:"overnight",nombre_huesped:draft.guest.trim(),email_huesped:draft.email?.trim()||null,telefono_huesped:draft.phone?.trim()||null,pais_huesped:draft.country?.trim()||null,cantidad_huespedes:totalGuests,canal_reserva:draft.channel||"Walk-in",codigo_canal:draft.voucher?.trim()||null,tarifa_noche:rate,noches:nights,subtotal,descuento_tipo:discountAmount>0?discountType:"none",descuento_valor:discountAmount>0?discountValue:0,descuento_importe:discountAmount,descuento_motivo:discountReason,descuento_origen:discountAmount>0?"manual":null,precio_total:finalTotal,moneda:draft.currency||"ARS",notas:draft.notes?.trim()||null,...taxPayload,cancellation_policy_id:policy.id,cancellation_policy_snapshot:policySnapshot(policy),mascotas:[],mascotas_total:0,servicios:[],pasajeros:[],vehiculos:0,cochera_total:0,estado:draft.status||"confirmada",no_show:false}
    const{data,error:rpcError}=await supabase.rpc("hl_create_reservation_atomic",{p_reservation:payload,p_payments:[]});if(rpcError)throw rpcError;await load(true);const names=selectedRooms.map(room=>room.nombre).join(", ");toast({title:roomIds.length>1?"Reserva grupal creada":"Reserva creada",message:`${draft.guest.trim()} · ${roomIds.length>1?`${roomIds.length} habitaciones (${names})`:`Habitación ${names}`} · ${nights} noche${nights===1?"":"s"}${discountAmount>0?` · descuento ${discountType==="percent"?`${discountValue}%`:money(discountAmount,draft.currency)} (${reasonLabel})`:""} · ${policy.name}.`});if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent("hl:pms-reservation-updated",{detail:{reservationId:data.id}}));return data
  },[propertyId,rooms,cancellationPolicies,windowStart,windowEndExclusive,load])

  return{rooms,reservations,blocks,cancellationPolicies,loading,error,setError,load,moveReservation,createReservation}
}