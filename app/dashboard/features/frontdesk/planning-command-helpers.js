import{addDays}from"../../core/formatters"

export function localNowKey(){const d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,"0"),day=String(d.getDate()).padStart(2,"0"),h=String(d.getHours()).padStart(2,"0"),min=String(d.getMinutes()).padStart(2,"0");return`${y}-${m}-${day}T${h}:${min}:00`}
export function occupancyStart(r){return String(r?.ocupacion_desde_local||`${r?.fecha_entrada||""}T${r?.hora_llegada_estimada||"14:00"}:00`).replace(" ","T")}
export function occupancyEnd(r){return String(r?.ocupacion_hasta_local||`${r?.fecha_salida||""}T${r?.hora_salida_estimada||"10:00"}:00`).replace(" ","T")}
export function parts(value){const raw=String(value||"").replace(" ","T"),[date,time="00:00:00"]=raw.split("T");return{date,time:time.slice(0,5)}}
export function isTentative(r){return String(r?.estado||"").toLowerCase()==="tentativa"}
export function tone(r,now){if(isTentative(r))return"tentative";if(r.estado==="finalizada")return"out";if(r.estado==="alojado")return"in";const start=occupancyStart(r),end=occupancyEnd(r);if(end&&now>=end)return"out";if(start&&end&&now>=start&&now<end)return"in";return"future"}
export function statusLabel(r,now){if(r?.no_show)return"NO SHOW";const status=String(r?.estado||"").toLowerCase();if(status==="cancelada")return"CANCELADA";if(status==="tentativa")return"TENTATIVA";if(status==="finalizada")return"OUT";if(status==="alojado")return"IN";const t=tone(r,now);if(t==="in")return"IN";if(t==="out")return"OUT";return"FUTURA"}
export function channelLabel(value){const raw=String(value||"Directa").trim(),channel=raw.toLowerCase();if(channel.includes("booking"))return"BOOKING.COM";if(channel.includes("expedia"))return"EXPEDIA";if(channel.includes("airbnb"))return"AIRBNB";if(channel.includes("motor"))return"MOTOR";if(channel.includes("agencia"))return"AGENCIA";if(channel==="directa")return"DIRECTA";if(channel.includes("teléfono")||channel.includes("telefono")||channel.includes("whatsapp")||channel.includes("walk-in"))return raw.toUpperCase();return raw.toUpperCase()}
export function channelColor(value){const v=String(value||"").toLowerCase();if(v.includes("booking"))return"#2868dc";if(v.includes("expedia"))return"#f3a31c";if(v.includes("airbnb"))return"#ef5f6c";if(v.includes("agencia"))return"#8064d8";if(v.includes("motor"))return"#0eaa83";if(v.includes("direct")||v.includes("whatsapp")||v.includes("tel")||v.includes("walk"))return"#11a67c";return"#8290a4"}
export function dayName(d){return new Date(`${d}T12:00:00`).toLocaleDateString("es-AR",{weekday:"short"}).replace(".","")}
export function monthName(d){return new Date(`${d}T12:00:00`).toLocaleDateString("es-AR",{month:"short"}).replace(".","")}
export function halfRange(r,days){const first=days[0],lastExclusive=addDays(days.at(-1),1),maxLine=days.length*2+1,start=parts(occupancyStart(r)),end=parts(occupancyEnd(r));let startLine;if(start.date<first)startLine=1;else{const i=days.indexOf(start.date);startLine=i<0?1:i*2+(start.time<"12:00"?1:2)}let endLine;if(end.date>=lastExclusive)endLine=maxLine;else{const i=days.indexOf(end.date);endLine=i<0?maxLine:i*2+(end.time<="12:00"?2:3)}startLine=Math.max(1,Math.min(maxLine-1,startLine));endLine=Math.max(startLine+1,Math.min(maxLine,endLine));return{startLine,endLine}}
export function stayKind(r){return r?.tipo_estadia==="day_use"?"DAY USE":"NOCHE HOTELERA"}
export function dayRangeLabel(days){if(!days.length)return"";const first=days[0],last=days.at(-1),a=new Date(`${first}T12:00:00`),b=new Date(`${last}T12:00:00`);if(a.getMonth()===b.getMonth())return`${a.getDate()}–${b.getDate()} ${monthName(last)}`;return`${a.getDate()} ${monthName(first)} – ${b.getDate()} ${monthName(last)}`}
export function overlapDay(r,day){return occupancyStart(r)<`${addDays(day,1)}T00:00:00`&&occupancyEnd(r)>`${day}T00:00:00`&&r.estado!=="cancelada"&&!r.no_show}
export function pct(part,total){return total?Math.round(part/total*100):0}
export function cleanPhone(value){return String(value||"").replace(/\D/g,"")}
export function roomStateLabel(value){const v=String(value||"").toLowerCase();if(v==="sucia")return"Sucia";if(v==="limpieza"||v==="en_limpieza")return"Limpieza";if(v==="limpia")return"Limpia";if(v==="inspeccion"||v==="inspeccionada")return"Inspección";if(v==="mantenimiento")return"Mant.";if(v==="fuera_servicio")return"Fuera";return"Lista"}
export function activeReservation(r){return r&&r.estado!=="cancelada"&&!r.no_show}
export function buildAttentionRoomIds(rooms,reservations,paymentMap,today){const ids=new Set(),tomorrow=addDays(today,1);rooms.forEach(room=>{if(["sucia","limpieza","en_limpieza","inspeccion","mantenimiento","fuera_servicio"].includes(String(room.estado||"").toLowerCase()))ids.add(String(room.id))});reservations.filter(activeReservation).forEach(r=>{if(!r.habitacion_id||r.fecha_salida<today||r.fecha_entrada>tomorrow)return;const balance=Math.max(0,Number(r.precio_total||0)-(paymentMap.get(String(r.id))||0));if(r.fecha_entrada===today||r.fecha_entrada===tomorrow||r.fecha_salida===today||isTentative(r)||balance>0)ids.add(String(r.habitacion_id))});return ids}
export function buildLensStats(reservations,paymentMap,today,attention){const live=reservations.filter(activeReservation),arrivals=live.filter(r=>r.fecha_entrada===today&&r.estado!=="alojado").length,departures=live.filter(r=>r.fecha_salida===today&&r.estado!=="finalizada").length,debt=live.filter(r=>r.fecha_salida>=today&&r.fecha_entrada<=addDays(today,1)&&Math.max(0,Number(r.precio_total||0)-(paymentMap.get(String(r.id))||0))>0).length;return{arrivals,departures,debt,attention}}
