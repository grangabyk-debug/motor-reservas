"use client"

import{addDays,money,shortDate}from"../../core/formatters"
import{tentativeCountdown}from"../../services/tentatives"
import RoomOperations from"./RoomOperations"
import cc from"./command-center.module.css"

const cleanPhone=value=>String(value||"").replace(/\D/g,"")
const isTentative=r=>String(r?.estado||"").toLowerCase()==="tentativa"
function tone(r,now){if(isTentative(r))return"tentative";if(r.estado==="finalizada")return"out";if(r.estado==="alojado")return"in";const start=String(r?.ocupacion_desde_local||`${r?.fecha_entrada||""}T${r?.hora_llegada_estimada||"14:00"}:00`).replace(" ","T"),end=String(r?.ocupacion_hasta_local||`${r?.fecha_salida||""}T${r?.hora_salida_estimada||"10:00"}:00`).replace(" ","T");if(end&&now>=end)return"out";if(start&&end&&now>=start&&now<end)return"in";return"future"}
function statusLabel(r,now){if(r?.no_show)return"NO SHOW";const status=String(r?.estado||"").toLowerCase();if(status==="cancelada")return"CANCELADA";if(status==="tentativa")return"TENTATIVA";if(status==="finalizada")return"OUT";if(status==="alojado")return"IN";const t=tone(r,now);if(t==="in")return"IN";if(t==="out")return"OUT";return"FUTURA"}
function channelLabel(value){const raw=String(value||"Directa").trim(),v=raw.toLowerCase();if(v.includes("booking"))return"BOOKING.COM";if(v.includes("expedia"))return"EXPEDIA";if(v.includes("airbnb"))return"AIRBNB";if(v.includes("motor"))return"MOTOR";if(v.includes("agencia"))return"AGENCIA";if(v==="directa")return"DIRECTA";return raw.toUpperCase()}

export default function PlanningInspector({selected,selectedRoom,selectedTotal,selectedPaid,selectedBalance,rooms,reservations,tapMoveId,onToggleMove,onClone,onSplit,onOpen,onClose,now}){
  if(!selected)return null
  const phone=cleanPhone(selected.telefono_huesped),tentative=isTentative(selected)
  return <aside className={cc.inspector}><header><div className={cc.guestIdentity}><span>{String(selected.nombre_huesped||"H").split(/\s+/).slice(0,2).map(x=>x[0]).join("").toUpperCase()}</span><div><small>RESERVA {selected.numero_reserva||selected.id}{selected.stay_chain_id?` · TRAMO ${selected.stay_segment_index||""}`:""}</small><h3>{selected.nombre_huesped}</h3><p>{selectedRoom?.nombre||"Sin habitación"} · {channelLabel(selected.canal_reserva)}</p></div></div><div className={cc.inspectorActions}><button onClick={()=>phone&&window.open(`https://wa.me/${phone}`,"_blank")} disabled={!phone}>WhatsApp</button><RoomOperations reservation={selected} rooms={rooms} reservations={reservations}/><button onClick={onToggleMove}>{String(tapMoveId)===String(selected.id)?"Cancelar mover":"Mover"}</button><button onClick={onClone}>Duplicar reserva</button>{selected.tipo_estadia!=="day_use"&&selected.fecha_salida>addDays(selected.fecha_entrada,1)&&<button onClick={onSplit}>Dividir estadía</button>}<button onClick={()=>onOpen(selected)}>Abrir ficha completa</button><button className={cc.closeInspector} onClick={onClose}>×</button></div></header><div className={cc.inspectorBody}><section><small>LLEGADA</small><b>{shortDate(selected.fecha_entrada)} · {selected.hora_llegada_estimada||"14:00"}</b></section><section><small>SALIDA</small><b>{shortDate(selected.fecha_salida)} · {selected.hora_salida_estimada||"10:00"}</b></section><section><small>OCUPACIÓN</small><b>{selected.cantidad_huespedes||1} huésped(es)</b></section><section><small>ESTADO</small><b className={tentative?cc.tentativeText:""}>{tentative?`TENTATIVA · ${tentativeCountdown(selected.tentative_expires_at)}`:statusLabel(selected,now)}</b></section><section className={cc.account}><div><small>CUENTA TOTAL</small><b>{money(selectedTotal,selected.moneda)}</b></div><div><small>PAGADO</small><b>{money(selectedPaid,selected.moneda)}</b></div><div><small>PENDIENTE</small><b className={selectedBalance>0?cc.balanceDue:""}>{money(selectedBalance,selected.moneda)}</b></div></section><section className={cc.note}><small>{tentative?"NOTA DE TENTATIVA":"NOTAS DEL HUÉSPED"}</small><p>{tentative?selected.tentative_note||"Sin nota de seguimiento.":selected.notas||"Sin notas cargadas."}</p></section></div></aside>
}
