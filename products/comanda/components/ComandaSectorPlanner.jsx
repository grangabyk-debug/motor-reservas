"use client";

import {useCallback,useEffect,useMemo,useState} from "react";
import {supabase} from "../../../lib/supabase";
import ui from "../styles/comanda-sector-planner.module.css";

const clamp=(n,min,max)=>Math.max(min,Math.min(max,Number(n)||min));
const SHAPES=["square","round","diamond"];
const shapeIndex=shape=>Math.max(0,SHAPES.indexOf(shape));
const shapeLabel=shape=>shape==="round"?"Circular":shape==="diamond"?"Rombo":"Cuadrada";
function text(el){return el?.textContent?.replace(/\s+/g," ").trim()||""}
function legacyWorkspace(){const headings=[...document.querySelectorAll("h1,h2,h3")].filter(x=>!x.closest('[data-sector-planner="1"]'));const h=headings.find(x=>text(x).toLowerCase().includes("mantenimiento de sectores"));return h?.closest('[class*="workspace"]')||h?.parentElement?.parentElement||null}

export default function ComandaSectorPlanner(){
 const [active,setActive]=useState(false),[branchId,setBranchId]=useState(null),[accountId,setAccountId]=useState(null),[sectors,setSectors]=useState([]),[tables,setTables]=useState([]),[sectorId,setSectorId]=useState(null),[modal,setModal]=useState(null),[saving,setSaving]=useState(false),[notice,setNotice]=useState(""),[dragId,setDragId]=useState(null);
 const sector=sectors.find(s=>s.id===sectorId)||null;const cols=clamp(sector?.grid_cols||8,2,18),rows=clamp(sector?.grid_rows||6,2,14),scale=clamp(sector?.cell_size||88,60,100);
 const load=useCallback(async id=>{if(!id)return;const [{data:branch},{data:ss,error:se},{data:tt,error:te}]=await Promise.all([supabase.from("comanda_branches").select("account_id").eq("id",id).maybeSingle(),supabase.from("comanda_sectors").select("*").eq("branch_id",id).eq("active",true).order("sort_order").order("created_at"),supabase.from("comanda_tables").select("*").eq("branch_id",id).eq("active",true).order("created_at")]);if(se||te){setNotice(se?.message||te?.message||"No se pudo cargar el salón");return}setAccountId(branch?.account_id||null);setSectors(ss||[]);setTables(tt||[]);setSectorId(old=>(ss||[]).some(s=>s.id===old)?old:ss?.[0]?.id||null)},[]);
 useEffect(()=>{const id=sessionStorage.getItem("comanda_branch");setBranchId(id);load(id);let hidden=null;const detect=()=>{const w=legacyWorkspace();const yes=!!w;setActive(yes);if(w){hidden=w;w.style.display="none"}else if(hidden){hidden.style.display="";hidden=null}};detect();const mo=new MutationObserver(detect);mo.observe(document.body,{subtree:true,childList:true,characterData:true});return()=>{mo.disconnect();if(hidden)hidden.style.display=""}},[load]);
 const sectorTables=useMemo(()=>tables.filter(t=>t.sector_id===sectorId),[tables,sectorId]);
 const occupied=useMemo(()=>{const map=new Map(),fallback=[];for(const t of sectorTables){const x=Number(t.pos_x),y=Number(t.pos_y);if(Number.isInteger(x)&&Number.isInteger(y)&&x>=0&&x<cols&&y>=0&&y<rows&&!map.has(`${x}:${y}`))map.set(`${x}:${y}`,t);else fallback.push(t)}let i=0;for(const t of fallback){while(i<cols*rows&&map.has(`${i%cols}:${Math.floor(i/cols)}`))i++;if(i<cols*rows){map.set(`${i%cols}:${Math.floor(i/cols)}`,t);i++}}return map},[sectorTables,cols,rows]);
 async function patchSector(patch){if(!sector)return;setSectors(v=>v.map(s=>s.id===sector.id?{...s,...patch}:s));const {error}=await supabase.from("comanda_sectors").update(patch).eq("id",sector.id);if(error){setNotice(error.message);load(branchId)}}
 async function addTable(form){if(!accountId||!branchId||!sector)return;setSaving(true);const number=String(form.number||sectorTables.length+1);const payload={account_id:accountId,branch_id:branchId,sector_id:sector.id,number,name:form.name||`Mesa ${number}`,seats:Number(form.seats||4),pos_x:form.x,pos_y:form.y,width:1,height:1,shape:form.shape||"square",table_type:"table",active:true};const {data,error}=await supabase.from("comanda_tables").insert(payload).select().single();setSaving(false);if(error){setNotice(error.message);return}setTables(v=>[...v,data]);setModal(null)}
 async function updateTable(id,patch){setSaving(true);const {data,error}=await supabase.from("comanda_tables").update(patch).eq("id",id).select().single();setSaving(false);if(error){setNotice(error.message);return}setTables(v=>v.map(t=>t.id===id?data:t));setModal(null)}
 async function moveTable(id,x,y){if(occupied.get(`${x}:${y}`))return;const {data,error}=await supabase.from("comanda_tables").update({pos_x:x,pos_y:y}).eq("id",id).select().single();if(error){setNotice(error.message);return}setTables(v=>v.map(t=>t.id===id?data:t));setNotice(`Mesa ${data.number||data.name} movida`)}
 async function deleteTable(id){setSaving(true);const {error}=await supabase.from("comanda_tables").update({active:false}).eq("id",id);setSaving(false);if(error){setNotice(error.message);return}setTables(v=>v.filter(t=>t.id!==id));setModal(null)}
 async function addSector(){if(!accountId||!branchId)return;setSaving(true);const {data,error}=await supabase.from("comanda_sectors").insert({account_id:accountId,branch_id:branchId,name:`Sector ${sectors.length+1}`,sort_order:sectors.length+1,active:true,sector_type:"salon",grid_cols:8,grid_rows:6,cell_size:88}).select().single();setSaving(false);if(error){setNotice(error.message);return}setSectors(v=>[...v,data]);setSectorId(data.id);setModal({type:"sector",name:data.name,newlyCreated:true})}
 async function deleteSector(){if(!sector||sectors.length<=1){setNotice("Debe quedar al menos un sector activo.");return}if(!confirm(`¿Eliminar el sector ${sector.name} y desactivar sus mesas?`))return;setSaving(true);await supabase.from("comanda_tables").update({active:false}).eq("sector_id",sector.id);const {error}=await supabase.from("comanda_sectors").update({active:false}).eq("id",sector.id);setSaving(false);if(error){setNotice(error.message);return}const next=sectors.filter(s=>s.id!==sector.id);setSectors(next);setTables(v=>v.filter(t=>t.sector_id!==sector.id));setSectorId(next[0]?.id||null);setModal(null)}
 if(!active||!sector)return null;
 const cells=Array.from({length:cols*rows},(_,i)=>({x:i%cols,y:Math.floor(i/cols)}));
 return <div className={ui.layer} data-sector-planner="1"><div className={ui.header}><div><span>CONFIGURACIÓN DEL SALÓN</span><h1>Mantenimiento de sectores y mesas</h1><p>Armá el plano por celdas. Todo se adapta al tamaño disponible sin scroll horizontal.</p></div><button className={ui.primary} onClick={addSector}>Nuevo sector</button></div><div className={ui.tabs}>{sectors.map(s=><button key={s.id} className={s.id===sectorId?ui.activeTab:""} onClick={()=>setSectorId(s.id)}>{s.name}</button>)}</div><div className={ui.body}><section className={ui.boardWrap}><div className={ui.board} style={{gridTemplateColumns:`repeat(${cols}, minmax(0,1fr))`,gridTemplateRows:`repeat(${rows}, minmax(0,1fr))`,"--cellScale":scale/100}}>{cells.map(({x,y})=>{const t=occupied.get(`${x}:${y}`),shape=t?.shape||"square";return <button key={`${x}-${y}`} draggable={!!t} onDragStart={e=>{if(!t)return;e.dataTransfer.effectAllowed="move";setDragId(t.id)}} onDragEnd={()=>setDragId(null)} onDragOver={e=>{if(dragId&&!t){e.preventDefault();e.dataTransfer.dropEffect="move"}}} onDrop={e=>{if(dragId&&!t){e.preventDefault();moveTable(dragId,x,y);setDragId(null)}}} className={`${ui.cell} ${t?ui.filled:""} ${shape==="round"?ui.round:""} ${shape==="diamond"?ui.diamond:""} ${dragId&&!t?ui.dropTarget:""}`} onClick={()=>t?setModal({type:"table",table:t,x,y}):setModal({type:"newTable",x,y})}>{t?<><strong>Mesa {t.number||t.name}</strong><small>{t.seats||4} lugares</small></>:<><b>＋</b><span>Agregar mesa</span></>}</button>})}</div></section><aside className={ui.panel}><div className={ui.panelHead}><div><small>SECTOR ACTIVO</small><strong>{sector.name}</strong></div><button onClick={()=>setModal({type:"sector",name:sector.name})}>Editar sector</button></div><label><span>Cantidad de columnas <b>{cols}</b></span><input type="range" min="2" max="18" value={cols} onChange={e=>patchSector({grid_cols:Number(e.target.value)})}/></label><label><span>Cantidad de filas <b>{rows}</b></span><input type="range" min="2" max="14" value={rows} onChange={e=>patchSector({grid_rows:Number(e.target.value)})}/></label><label><span>Tamaño de mesas <b>{scale}%</b></span><input type="range" min="60" max="100" value={scale} onChange={e=>patchSector({cell_size:Number(e.target.value)})}/></label><div className={ui.hint}>El tamaño se modifica de forma proporcional en ancho y alto. También podés arrastrar una mesa y soltarla sobre cualquier celda libre.</div><div className={ui.legend}><span><i className={ui.dotTable}/>Mesa creada</span><span><i className={ui.dotEmpty}/>Celda libre</span></div></aside></div>{modal&&<div className={ui.overlay} onMouseDown={e=>e.target===e.currentTarget&&setModal(null)}><div className={ui.modal}><div className={ui.modalHead}><div><small>{modal.type==="sector"?"SECTOR":"MESA"}</small><h3>{modal.type==="newTable"?"Agregar mesa":modal.type==="table"?`Editar mesa ${modal.table.number||""}`:"Editar sector"}</h3></div><button onClick={()=>setModal(null)}>×</button></div>{modal.type==="sector"?<SectorForm initial={modal} disabled={saving} canDelete={!modal.newlyCreated&&sectors.length>1} onDelete={deleteSector} onCancel={()=>setModal(null)} onSave={async f=>{setSaving(true);const {data,error}=await supabase.from("comanda_sectors").update({name:f.name}).eq("id",sector.id).select().single();setSaving(false);if(error){setNotice(error.message);return}setSectors(v=>v.map(s=>s.id===sector.id?data:s));setModal(null)}}/>:modal.type==="newTable"?<TableForm initial={{number:String(sectorTables.length+1),name:"",seats:4,shape:"square",x:modal.x,y:modal.y}} disabled={saving} onCancel={()=>setModal(null)} onSave={addTable}/>:<TableForm initial={{...modal.table,x:modal.x,y:modal.y}} disabled={saving} onCancel={()=>setModal(null)} onDelete={()=>deleteTable(modal.table.id)} onSave={f=>updateTable(modal.table.id,{number:String(f.number),name:f.name||`Mesa ${f.number}`,seats:Number(f.seats),shape:f.shape,pos_x:f.x,pos_y:f.y})}/>}</div></div>}{notice&&<div className={ui.toast} onClick={()=>setNotice("")}>{notice}</div>}</div>
}
function TableForm({initial,onSave,onCancel,onDelete,disabled}){const [f,setF]=useState(initial);const set=(k,v)=>setF(x=>({...x,[k]:v}));const stylePos=shapeIndex(f.shape);return <form onSubmit={e=>{e.preventDefault();onSave(f)}}><div className={ui.form}><label className={ui.full}>Número de mesa<input value={f.number||""} onChange={e=>set("number",e.target.value)} required autoFocus/></label><label className={ui.full}>Nombre / etiqueta<input value={f.name||""} onChange={e=>set("name",e.target.value)} placeholder="Opcional"/></label><label className={ui.full}>Capacidad de comensales<input type="number" min="1" max="30" value={f.seats||4} onChange={e=>set("seats",e.target.value)} required/></label><div className={`${ui.full} ${ui.styleField}`}><div className={ui.styleTitle}><span>Estética</span><b>{shapeLabel(f.shape)}</b></div><input className={ui.styleSlider} type="range" min="0" max="2" step="1" value={stylePos} onChange={e=>set("shape",SHAPES[Number(e.target.value)])}/><div className={ui.styleChoices}><span><i className={ui.shapeSquare}/>Cuadrada</span><span><i className={ui.shapeRound}/>Circular</span><span><i className={ui.shapeDiamond}/>Rombo</span></div></div></div><div className={ui.modalFoot}>{onDelete&&<button type="button" className={ui.danger} onClick={onDelete}>Eliminar</button>}<span/><button type="button" onClick={onCancel}>Cerrar</button><button className={ui.primary} disabled={disabled}>Guardar</button></div></form>}
function SectorForm({initial,onSave,onCancel,onDelete,canDelete,disabled}){const [name,setName]=useState(initial.name);return <form onSubmit={e=>{e.preventDefault();onSave({name})}}><div className={ui.form}><label className={ui.full}>Nombre del sector<input value={name} onChange={e=>setName(e.target.value)} required autoFocus/></label></div><div className={ui.modalFoot}>{canDelete?<button type="button" className={ui.danger} onClick={onDelete}>Eliminar sector</button>:<span/>}<span/><button type="button" onClick={onCancel}>Cerrar</button><button className={ui.primary} disabled={disabled}>Guardar cambios</button></div></form>}
