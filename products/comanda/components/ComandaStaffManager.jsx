"use client";

import {useEffect,useMemo,useState} from "react";
import {supabase} from "../../../lib/supabase";
import s from "../styles/comanda-staff.module.css";

const TYPES=[["waiter","Mozo / Camarero"],["cook","Cocinero"],["cashier","Cajero"],["manager","Encargado"],["supervisor","Supervisor"],["delivery","Repartidor"],["bar","Bar"],["admin","Administrador"],["other","Otro"]];
const typeLabel=v=>TYPES.find(x=>x[0]===v)?.[1]||v||"Sin tipo";
function Modal({title,onClose,onSave,saving,children}){return <div className={s.overlay} onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className={s.modal} role="dialog" aria-modal="true"><header><h2>{title}</h2><button onClick={onClose}>×</button></header><div className={s.modalBody}>{children}</div><footer><button className={s.secondary} onClick={onClose}>Cancelar</button><button className={s.primary} disabled={saving} onClick={onSave}>{saving?"Guardando…":"Guardar cambios"}</button></footer></section></div>}

export default function ComandaStaffManager(){
 const [open,setOpen]=useState(false),[rows,setRows]=useState([]),[turns,setTurns]=useState([]),[branch,setBranch]=useState(null),[users,setUsers]=useState([]),[workstations,setWorkstations]=useState([]),[staffWs,setStaffWs]=useState([]),[loading,setLoading]=useState(false),[dialog,setDialog]=useState(null),[saving,setSaving]=useState(false),[query,setQuery]=useState("");
 const branchId=typeof window!=="undefined"?sessionStorage.getItem("comanda_branch"):null;const access=typeof window!=="undefined"?sessionStorage.getItem("comanda_access_level"):null;const canAdmin=["owner","superadmin"].includes(access)||document?.documentElement?.dataset?.comandaRole==="principal";
 async function load(){if(!branchId)return;setLoading(true);const {data:b}=await supabase.from("comanda_branches").select("id,name,account_id").eq("id",branchId).maybeSingle();if(!b){setLoading(false);return}const [{data:staff},{data:turnRows},{data:profiles},{data:ws},{data:links}]=await Promise.all([supabase.from("comanda_staff").select("id,account_id,branch_id,name,staff_type,active,created_at,user_id,title,turn_id,updated_at,email,phone").eq("branch_id",branchId).order("name"),supabase.from("comanda_turns").select("id,name,start_time,end_time,active").eq("branch_id",branchId).eq("active",true).order("sort_order"),supabase.from("comanda_user_profiles").select("user_id,full_name,email,access_level,active").eq("account_id",b.account_id).eq("active",true).order("full_name"),supabase.from("comanda_workstations").select("id,name,kind,active").eq("branch_id",branchId).eq("active",true).order("name"),supabase.from("comanda_staff_workstations").select("staff_id,workstation_id").eq("account_id",b.account_id)]);setRows(staff||[]);setTurns(turnRows||[]);setUsers(profiles||[]);setWorkstations(ws||[]);setStaffWs(links||[]);setBranch(b);setLoading(false)}
 useEffect(()=>{const onClick=e=>{const b=e.target?.closest?.("button");if(!b)return;const t=b.textContent?.replace(/\s+/g," ").trim();if(t==="Funcionarios"){setOpen(true);setTimeout(load,0)}if(open&&["Usuarios","Cajas","Puestos","Sectores","Cocinas","Impresoras","Tickets","Notificaciones","Módulos y pagos","Mi cuenta"].includes(t))setOpen(false)};document.addEventListener("click",onClick,true);return()=>document.removeEventListener("click",onClick,true)},[open,branchId]);
 const filtered=useMemo(()=>{const q=query.toLowerCase().trim();return rows.filter(r=>!q||`${r.name} ${typeLabel(r.staff_type)} ${r.title||""} ${r.email||""}`.toLowerCase().includes(q))},[rows,query]);
 const wsIdsFor=id=>staffWs.filter(x=>x.staff_id===id).map(x=>x.workstation_id);const newStaff=()=>setDialog({mode:"new",data:{name:"",staff_type:"waiter",title:"",turn_id:"",active:true,email:"",phone:"",user_id:"",workstation_ids:[]}});const edit=row=>setDialog({mode:"edit",row,data:{name:row.name||"",staff_type:row.staff_type||"waiter",title:row.title||"",turn_id:row.turn_id||"",active:row.active!==false,email:row.email||"",phone:row.phone||"",user_id:row.user_id||"",workstation_ids:wsIdsFor(row.id)}});const toggleWs=id=>setDialog(d=>({...d,data:{...d.data,workstation_ids:d.data.workstation_ids.includes(id)?d.data.workstation_ids.filter(x=>x!==id):[...d.data.workstation_ids,id]}}));
 async function save(){if(!dialog?.data?.name?.trim()||!branch)return;setSaving(true);try{const d=dialog.data;const payload={account_id:branch.account_id,branch_id:branch.id,name:d.name.trim(),staff_type:d.staff_type,title:d.title?.trim()||null,turn_id:d.turn_id||null,active:d.active!==false,email:d.email.trim()||null,phone:d.phone.trim()||null,user_id:d.user_id||null,updated_at:new Date().toISOString()};let staffId=dialog.row?.id;if(dialog.mode==="edit"){const {error}=await supabase.from("comanda_staff").update(payload).eq("id",staffId);if(error)throw error}else{const {data,error}=await supabase.from("comanda_staff").insert(payload).select("id").single();if(error)throw error;staffId=data.id}await supabase.from("comanda_staff_workstations").delete().eq("staff_id",staffId);if(d.workstation_ids.length){const {error}=await supabase.from("comanda_staff_workstations").insert(d.workstation_ids.map(id=>({account_id:branch.account_id,staff_id:staffId,workstation_id:id})));if(error)throw error}setDialog(null);await load()}catch(e){alert(e.message||"No se pudo guardar el funcionario")}finally{setSaving(false)}}
 async function remove(row){if(!confirm(`¿Desactivar a ${row.name}?`))return;await supabase.from("comanda_staff").update({active:false,updated_at:new Date().toISOString()}).eq("id",row.id);await load()}
 if(!open||!canAdmin)return null;
 return <main className={s.manager} data-staff-manager="1"><header className={s.header}><div><span>CONFIGURACIÓN</span><h1>Funcionarios</h1><p>Empleados reales de {branch?.name||"la sucursal"}. El usuario de acceso es opcional.</p></div><div className={s.actions}><button className={s.secondary} onClick={()=>setOpen(false)}>Volver a Configuración</button><button className={s.primary} onClick={newStaff}>Nuevo funcionario</button></div></header><div className={s.toolbar}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nombre, función o email…"/><div><strong>{rows.filter(x=>x.active).length}</strong><span> activos</span></div></div><section className={s.card}>{loading?<div className={s.empty}>Cargando funcionarios…</div>:filtered.length?<div className={s.tableWrap}><table><thead><tr><th>Nombre</th><th>Tipo</th><th>Turno</th><th>Puestos</th><th>Usuario vinculado</th><th>Estado</th><th></th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td><strong>{r.name}</strong>{r.title&&<small>{r.title}</small>}</td><td>{typeLabel(r.staff_type)}</td><td>{turns.find(t=>t.id===r.turn_id)?.name||"Sin turno"}</td><td>{wsIdsFor(r.id).map(id=>workstations.find(w=>w.id===id)?.name).filter(Boolean).join(", ")||"Sin asignar"}</td><td>{users.find(u=>u.user_id===r.user_id)?.email||"No vinculado"}</td><td><span className={r.active?s.active:s.inactive}>{r.active?"Activo":"Inactivo"}</span></td><td><div className={s.rowActions}><button onClick={()=>edit(r)}>Editar</button><button className={s.danger} onClick={()=>remove(r)}>Eliminar</button></div></td></tr>)}</tbody></table></div>:<div className={s.empty}>No hay funcionarios cargados.</div>}</section>{dialog&&<Modal title={dialog.mode==="new"?"Nuevo funcionario":"Editar funcionario"} onClose={()=>setDialog(null)} onSave={save} saving={saving}><div className={s.formGrid}><label><span>Nombre del empleado</span><input value={dialog.data.name} onChange={e=>setDialog(d=>({...d,data:{...d.data,name:e.target.value}}))}/></label><label><span>Tipo de funcionario</span><select value={dialog.data.staff_type} onChange={e=>setDialog(d=>({...d,data:{...d.data,staff_type:e.target.value}}))}>{TYPES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label><span>Email</span><input type="email" value={dialog.data.email} onChange={e=>setDialog(d=>({...d,data:{...d.data,email:e.target.value}}))}/></label><label><span>Teléfono</span><input value={dialog.data.phone} onChange={e=>setDialog(d=>({...d,data:{...d.data,phone:e.target.value}}))}/></label><label><span>Cargo / descripción</span><input value={dialog.data.title} onChange={e=>setDialog(d=>({...d,data:{...d.data,title:e.target.value}}))} placeholder="Opcional"/></label><label><span>Turno</span><select value={dialog.data.turn_id} onChange={e=>setDialog(d=>({...d,data:{...d.data,turn_id:e.target.value}}))}><option value="">Sin turno asignado</option>{turns.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label className={s.formGrid} style={{gridColumn:"1/-1"}}><span>Usuario del sistema vinculado (opcional)</span><select value={dialog.data.user_id} onChange={e=>setDialog(d=>({...d,data:{...d.data,user_id:e.target.value}}))}><option value="">Sin usuario vinculado</option>{users.map(u=><option key={u.user_id} value={u.user_id}>{u.full_name||u.email} · {u.email}</option>)}</select></label><div style={{gridColumn:"1/-1"}}><strong>Puestos en los que trabaja</strong><div className={s.rowActions} style={{marginTop:8,flexWrap:"wrap"}}>{workstations.map(ws=><button key={ws.id} type="button" className={dialog.data.workstation_ids.includes(ws.id)?s.primary:s.secondary} onClick={()=>toggleWs(ws.id)}>{ws.name}</button>)}</div></div><label className={s.check}><input type="checkbox" checked={dialog.data.active} onChange={e=>setDialog(d=>({...d,data:{...d.data,active:e.target.checked}}))}/><span>Funcionario activo</span></label></div></Modal>}</main>
}
