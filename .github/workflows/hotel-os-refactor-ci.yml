name: Hotel OS Refactor CI

on:
  push:
    branches:
      - refactor/hotel-os-modular-foundation
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: npm run check:product-boundaries
      - run: npm run check:hotel-architecture
      - run: npm run build
